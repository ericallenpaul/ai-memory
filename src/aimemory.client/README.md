# aimemory.client (RETIRED for active development)

> **Status:** Retired for active development as of 2026-05-03. Superseded by `apps/desktop/`.
>
> This React + Vite SPA was the in-browser web UI served by `AIMemory.Api`. It is being
> replaced by the Tauri desktop app at `apps/desktop/`, which reuses some of the same
> patterns (theming, layout, API client) but is locally-installed rather than browser-served.
>
> Kept in the repo as a reference and to keep the embedded SPA build working until Tauri
> reaches feature parity for the code-indexer surface.

See `.claude/memory-bank/main/plans/main-2026-05-03-tauri-code-indexer.md` for the migration plan.
